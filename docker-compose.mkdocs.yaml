services:
  mkdocs:
    image: ${DOCKER_REGISTRY}/${APP_ENV:-app}_mkdocs:latest
    pull_policy: always
    container_name: ${APP_ENV:-app}_mkdocs
    hostname: ${APP_ENV:-app}_mkdocs
    restart: unless-stopped
    environment:
      SITE_URL: "https://features.ai-center.online/docs/"
    stdin_open: true
    tty: true
    networks:
      - ${APP_ENV:-app}-net
